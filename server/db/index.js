/*const mongoose = require('mongoose')

mongoose
    .connect('mongodb+srv://user_1:rootpassword@directory.rbx2l.mongodb.net/directory?retryWrites=true&w=majority', {useNewUrlParser: true})
    .catch(e => {
        console.error('Connection error', e.message)
    })W

const db = mongoose.connection;

module.exports = db*/