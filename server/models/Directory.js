/*const mongoose = require('mongoose')

const DirectorySchema = new mongoose.Schema({
    type: {
        type: String,
        required: true,
    },
});

const Directory = mongoose.model("DirectorySchema", DirectorySchema);
module.export = Directory;*/